[package]
name = "cli"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "CLI binary for nexus"

[dependencies]
core = {path = "../core"}
parse = {path = "../parse"}
report = {path = "../report"}
