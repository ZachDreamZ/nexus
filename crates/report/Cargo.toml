[package]
name = "report"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Output formatters for nexus (terminal, JSON, Mermaid)"

[dependencies]
core = {path = "../core"}
